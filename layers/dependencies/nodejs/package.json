{
  "name": "tea-utils",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "npm install && tsc && cp -r node_modules/* nodejs/node_modules && zip -r nodejs.zip nodejs && rm -rf nodejs && cd .. && mv nodejs/nodejs.zip ."
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "uuid": "^8.3.2"
  },
  "devDependencies": {
    "@types/node": "^14.14.31",
    "typescript": "^4.2.2"
  }
}
